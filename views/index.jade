extends layout

block content
    h1= title

    p.
        Find connections between websites.
    form(action='/', method='POST')
        //- | Search Cluster:
        input(class='clusterSearch', type='search', name='clusterSearch', placeholder='http://')
        br
        input(type='submit', value='Cluster Search')

    p
        br
        strong
            a(href='/websites') Potter about.
